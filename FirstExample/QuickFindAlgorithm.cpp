#include "QuickFindAlgorithm.h"

//__________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________
void QuickFindAlgorithm::Initialize(int n)
{
	if(!elements.empty())
		elements.clear();

	for (int i = 0; i < n; i++)
		elements.push_back(i);
}

//__________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________
bool QuickFindAlgorithm::Connected(int p, int q)
{
	return elements[p] == elements[q];
}

//__________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________
void QuickFindAlgorithm::Union(int p, int q)
{
	int totalElements = elements.size();
	int pId = elements[p];
	int qId = elements[q];

	for(int i = 0; i < totalElements; i++)
	{
		if(elements[i] == pId)
			elements[i] = qId;
	}
}

//__________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________
int QuickFindAlgorithm::Components()
{
	return 1;
}
